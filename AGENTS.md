# للوكلاء العاملين على هذا المستودع

> يقرأ Codex هذا الملف تلقائياً. وClaude يقرأ `CLAUDE.md`.
> **الملفان يقولان الشيء نفسه** — والمعايير الكاملة في `CLAUDE.md`، فاقرأه.

---

## 🔴 ابدأ جلستك بهذا — قبل أي سطر

```bash
team/bin/bridge read codex     # رسائل مفتوحة إليك
team/bin/bridge board          # الحالة · من يملك ماذا · وما هو مجمّد
```

**من بدأ العمل بلا قراءة الاثنين عمل أعمى** — فأعاد ما فُعل، أو اصطدم بمجمّد،
أو تجاوز نطاقه.

---

## من يملك ماذا

| | النطاق |
|---|---|
| **codex** | البنية والتشغيل — Redis · Turnstile · قاعدة البيانات · Sentry · النشر |
| **claude** | `packages/` — القواعد والمحرّكات وخطّ الأنابيب · الوثائق |
| **الرئيس** | القرار · المفاتيح · المراجعة · التجربة على الموقع الحيّ |

**ملفات مختلفة، فلا تصادم.** ومن احتاج أن يلمس نطاق غيره: يطلبه في صندوقه أولاً.

---

## عند الانتهاء

```bash
team/bin/bridge send codex claude "ما أنجزتُه" <<'EOF'
النتيجة، والأمر الذي يُثبتها.
EOF
team/bin/bridge close codex CODEX-00X
```

**كل ادّعاء مقيس بالأمر الذي يُنتجه.** و«لم أجد» لا «لا يوجد».

## عند الحاجة لقرار

```bash
team/bin/bridge ask codex "السؤال"   # يرفع للرئيس ثم توقّف
```

للقرارات التي تغيّر المنتج أو السعر أو النطاق · وpush/merge/deploy ·
وتعارض لم يُحسم برسالتين. **وما دون ذلك: قرّروه بينكم ولا توقفوا.**

---

## سجلّ الأخطاء — اقرأه قبل أن تبني

`docs/LESSONS.md` — أربعة وعشرون خطأً وقع فعلاً في هذا المستودع، لكلٍّ
دليلُه والقاعدة التي تمنع تكراره. نصفها في نطاقك: أقفال، ومصائد الصدفة،
و`pipefail`، وبيانات الاعتماد.

وأضِف إليه: `team/bin/bridge lesson codex "العنوان"`.

---

## القواعد التسع — لا تُناقَش

كاملةً في `CLAUDE.md`. وأكثر ما يخصّك منها:

| # | القاعدة |
|---|---|
| 1 | **الطمس عند المصدر** — لا قيمة سرّ كاملة في قاعدة بيانات ولا سجلّ ولا استجابة ولا Sentry |
| 2 | **لا شبكة خارج `safeFetch`** — وأي استثناء يُعلَّل في الكود |
| 4 | **الكاش وحدّ الزائر قبل أي ترويج** — وهذا سبب وجود #3 و#4 |
| 6 | **لا رقم بلا مصدر** |
| 9 | **مسار API جديد لا يُدمج بلا اختبار** |
| 8 | **`strict: true` ولا `any`** |

**تغيير أيٍّ منها قرار الرئيس، لا قرار من يكتب الكود.**

---

## الاختبارات هي العقد

```bash
pnpm test        # يجب أن يبقى 0 فشل
pnpm typecheck   # يجب أن يبقى 0 أخطاء
```

اختبار يحرس **سلوكاً** لا شكل كود. لتغيير سلوك: غيّر الاختبار أولاً واشرح
السبب في رسالة الالتزام. **وحذف اختبار لأنه صار أحمر ليس إصلاحاً.**

---

## سير العمل

```
فرع من master  →  PR  →  CI أخضر  →  مراجعة  →  دمج بإذن الرئيس
```

⚠️ **الفرع المرجعي `master` لا `main`.** النسخة المحلية على هذا الجهاز
متأخرة — زامِن قبل أن تبدأ.

**لا push ولا merge ولا deploy إلّا بإذن صريح من الرئيس.**

---

## المفاتيح

في متغيّرات بيئة Vercel وGitHub Secrets.
**لا في المستودع، ولا في الجسر، ولا في رسالة.**

بلا مفاتيح يعمل الموقع في وضع تجريبي ويصرّح بذلك على الشاشة — فتستطيع بناء
كل شيء بلا فتح أي حساب.
