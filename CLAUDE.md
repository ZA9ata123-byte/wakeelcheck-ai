# معايير المشروع

هذا الملف يقرأه كل من يعمل في المستودع — مبرمجاً كان أو جلسة مساعد جديدة.
كُتب لأن السياق لا يعيش في ذاكرة أحد: يعيش هنا.

---

## القرار المعماري الحاكم

**الحزم في `packages/` لا تعرف شيئاً عن HTTP.** لا تستورد `next`، ولا ترى طلباً
ولا استجابة. تأخذ بيانات وتُرجع بيانات.

كل ما يلامس العالم الخارجي — شبكة، ساعة، مولّد معرّفات — يُحقن من الخارج.
لهذا يُختبر الفحص كاملاً بلا خادم ولا مفتاح ولا شبكة.

مخالفة هذا تعني أن جزءاً من المنتج صار غير قابل للاختبار. لا تفعل.

---

## القواعد الثمانية — لا تُناقَش

| # | القاعدة | لماذا |
|---|---|---|
| 1 | **الطمس عند المصدر** — لا تدخل قيمة سرّ كاملة قاعدةَ بيانات ولا سجلّاً ولا استجابة | مفاتيح دفع مئات المتاجر في جدول واحد هدفُ اختراق لا أصل |
| 2 | **لا شبكة خارج `safeFetch`** | هو ما يمنع SSRF، ويعيد التحقق عند كل إعادة توجيه |
| 3 | **لا نداء مباشر لـ ox-alpha** — فقط `llm.complete()` | معاينة مجانية لفترة محدودة؛ اختفاؤها يجب أن يكون تغييراً في البيئة |
| 4 | **الكاش وحدّ الزائر قبل أي ترويج** | حملة مؤثر واحدة بلا حدود تحرق آلاف الريالات |
| 5 | **`answerText` حرفي** — لا تنظيف ولا اقتصاص ولا إعادة صياغة | هو الدليل، وتعديله يُبطله |
| 6 | **لا رقم بلا مصدر** | المحرّك القديم عرض PageSpeed محسوباً بمعادلة ونسبه إلى Google |
| 7 | **الفحص العميق خلف `verified_at`** | نظام الجرائم المعلوماتية لا يستثني حسن النية |
| 8 | **`strict: true` ولا `any`** | أربعة من الأخطاء السبعة الأصلية كان المترجم سيرفضها |

تغيير أيٍّ منها قرار مالك المشروع، لا قرار من يكتب الكود.

---

## الاختبارات هي العقد

```bash
pnpm test        # يجب أن يبقى 0 فشل
pnpm typecheck   # يجب أن يبقى 0 أخطاء
```

اختبار يحرس سلوكاً، لا شكل كود. لتغيير سلوك: **غيّر الاختبار أولاً واشرح السبب
في رسالة الالتزام.** حذف اختبار لأنه صار أحمر ليس إصلاحاً.

كل قاعدة جاهزية لها اختباران — حالة ناجحة وحالة راسبة. واختبار
«كل قاعدة قابلة للإخفاق» يمنع عودة القواعد التي تنجح دائماً فتضخّم النتيجة.

---

## أشياء ثبت أنها تنكسر

مكتوبة هنا لأن كل واحدة كلّفت وقتاً فعلياً:

- **`\b` لا يعمل مع العربية.** بلا فحص المحرف المجاور يدوياً، «دار» تطابق داخل
  «مدارس» وتُنتج ذكراً وهمياً.
- **`robots.txt` يُحلَّل بالمجموعات لا بـ`includes()`.** المنطق القديم كان يُنجح
  متجراً يحظر كل البوتات ويُرسب متجراً يرحّب بـGPTBot.
- **قواعد المنتج تُقيَّم على صفحات المنتج.** الرئيسية لا تحمل `Product Schema`
  عادةً، وتقييمها عليها يُرسب كل متجر.
- **غياب القيمة ليس اتساقاً.** `null === null` جعل متجراً بلا أي سعر يجتاز فحص
  اتساق الأسعار.
- **قيم اختبارية تشبه أسراراً تُبنى وقت التشغيل.** كتابتها كنصوص حجب دفعاً كاملاً.
- **Next لا يمرّر معلومات المسار إلى تخطيط جذري واحد.** لغة الصفحة واتجاهها
  يحتاجان مجموعتَي مسارات، لكل واحدة جذرها.
- **`pkill -f "next start"` يطابق سطر أمر الصدفة نفسه** ويقتلها. استعمل نمطاً
  بقوسين: `nex[t]-server`.

---

## التصميم

الهوية في `docs/03-visual-system`. لا يُكتب لون في مكوّن — كل لون رمز مُعرَّف في
الوضعين الفاتح والداكن. العلامة (`mark.rival`) لا تُوضع إلا على اسم عاد من
الاستخراج، أي اسم تُحقّق من وجوده حرفياً في الإجابة.

لائحة «ما لا نفعله أبداً» في الوثيقة نفسها ملزمة مثل الباقي.

---

## سير العمل

```
فرع  →  PR  →  CI أخضر  →  مراجعة  →  دمج
```

CI يشغّل الاختبارات والأنواع والبناء، ويرفض أي نصّ يطابق نمط سرّ. لا يُدمج
شيء وCI أحمر.

المفاتيح تعيش في متغيّرات بيئة Vercel وGitHub Secrets. لا في المستودع، ولا في
رسالة، ولا في محادثة.

---

## الوثائق

| | |
|---|---|
| `docs/01-technical-roadmap` | المعمار وسبب كل اختيار |
| `docs/02-go-to-market` | الخدمات والأهداف والقنوات |
| `docs/03-visual-system` | الهوية — للمصمم |
| `docs/04-build-spec` | شجرة الملفات والعقود — للمبرمج |
| `docs/05-launch.md` | ما تبقّى قبل أول زائر |
