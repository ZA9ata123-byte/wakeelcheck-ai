{
  "name": "wakeelcheck",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@9.15.0",
  "scripts": {
    "typecheck": "tsc --noEmit -p packages/core && tsc --noEmit -p packages/fetcher && tsc --noEmit -p packages/readiness && tsc --noEmit -p packages/security && tsc --noEmit -p packages/llm && tsc --noEmit -p packages/visibility && tsc --noEmit -p packages/limits && tsc --noEmit -p packages/pipeline && tsc --noEmit -p packages/db && tsc --noEmit -p packages/engines && tsc --noEmit -p scripts",
    "test": "tsx --test packages/*/test/*.test.ts",
    "build:docs": "node scripts/build-docs.cjs",
    "specimen": "tsx scripts/make-specimen.ts && tsx scripts/render-specimen.ts"
  },
  "devDependencies": {
    "typescript": "^5.9.0",
    "@types/node": "^22.10.0",
    "tsx": "^4.19.0"
  }
}
